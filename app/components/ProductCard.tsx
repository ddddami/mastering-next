import React from 'react';
import AddToCartButton from './AddToCartButton';

const ProductCard = () => {
  return (
    <div>
      <p>ProductCard</p>
      <AddToCartButton />
    </div>
  );
};

export default ProductCard;
